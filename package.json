{
  "name": "kalpana-enterprises",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "preview": "npm run preview --prefix frontend"
  },
  "workspaces": [
    "frontend"
  ],
  "dependencies": {
    "chokidar": "^5.0.0",
    "multer": "^2.2.0",
    "pdf-lib": "^1.17.1",
    "sharp": "^0.35.3"
  }
}
